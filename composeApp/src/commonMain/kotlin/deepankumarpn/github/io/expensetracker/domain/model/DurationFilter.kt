package deepankumarpn.github.io.expensetracker.domain.model

/**
 * Enum representing duration filter options.
 */
enum class DurationFilter {
    DAILY,
    WEEKLY,
    MONTHLY,
    QUARTER_YEAR,
    HALF_YEAR,
    CUSTOM
}
